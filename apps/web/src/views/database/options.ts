// 资料库页选项/标签统一复用 @model-stacker/components（数据源：data 包显示表），
// 此处仅保留页面内的本地命名（产品状态在本页命名为 status*）。

export {
  commonStatusLabels,
  commonStatusOptions,
  commonStatusTagTypes,
  kindLabels,
  kindOptions,
  kindTagTypes,
  modelTypeLabels,
  modelTypeOptions,
  productStatusLabels as statusLabels,
  productStatusOptions as statusOptions,
  productStatusTagTypes as statusTagTypes,
  toolTypeLabels,
  toolTypeOptions,
} from '@model-stacker/components';