// 我的（堆积）页选项/标签统一复用 @model-stacker/components（数据源：data 包显示表），
// 此处仅保留页面内的本地命名（渠道 → purchaseChannel*，堆积状态 → stackStatus*）。

export {
  currencyLabels,
  currencyOptions,
  currencySymbols,
  kindLabels,
  kindTagTypes,
  purchaseChannelLabels as channelLabels,
  purchaseChannelOptions as channelOptions,
  stackStatusLabels as statusLabels,
  stackStatusOptions as statusOptions,
  stackStatusTagTypes as statusTagTypes,
} from '@model-stacker/components';